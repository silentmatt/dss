something { first: yes }
@include url(include/inc.dss);
element {
    border: const(foo);
    apply: includedClass;
}

@include 'include/header.dss' {
    width: 960px;
    background: #eeeeff;
    color: white;
}
